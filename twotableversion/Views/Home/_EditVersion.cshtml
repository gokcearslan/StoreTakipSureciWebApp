@*@model twotableversion.Models.UygulamalarModel

<div class="modal fade" id="editVersionModal" tabindex="-1" role="dialog" aria-labelledby="editVersionModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="editVersionModalLabel">Edit Version</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <!-- Your edit form here -->
                @using (Html.BeginForm("EditVersion", "Home", FormMethod.Post))
                {
                    @Html.HiddenFor(model => model.version)
                  
                    <button type="submit" class="btn btn-primary">Save</button>
                }
            </div>
        </div>
    </div>
</div>
*@